name: Demo Manual Workflow

on:
  workflow_dispatch:
    inputs:
      mensaje:
        description: 'Mensaje de texto'
        required: true
        type: string

jobs:
  imprimir-mensaje:
    runs-on: ubuntu-latest
    steps:
      - name: Imprimir por consola
        run: echo "${{ inputs.mensaje }}"
